int n := PI -1;
proc factorial(int n)int:
    int result := 0;

    while
	    n < 10
    do
	    write("Factorial of ", n, " is ", factorial(n), "\n");
    od;

    while
        write("Enter n, or >= 10 to exit: ");
        readln(n);
        -> n < 10
    do
        write("Factorial of ", n, " is ", factorial(n), "\n");
    od;

    ->
corp;

proc empty()int:
    int x := 5;
    -> x
corp;

proc main()int:
    write("Hello, world\n");
    -> 0
corp;
