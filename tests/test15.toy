proc in_str() string:
    string res;
    readln(res);
    -> res
corp;

proc main() int:
    string a, b;
    write("Fornisci una stringa: ");
    a := in_str();
    write("Fornisci un'altra stringa: ");
    b := in_str();
    write("Input: ", a, ", ", b, "\n");
    if a < b then
        write("a < b");
    elif a = b then
        write("a == b");
    elif a > b then
        write("a > b");
    else
        write("dead code");
    fi;
    -> 0
corp;
